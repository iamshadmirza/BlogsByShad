---
description: talk about what are global modules, how they work, where they are generally used.

- what are global module, Installing a package globally allows you to use the code in the package as a set of tools on your local computer.
- how to install, the attribute -g, explain the word global here
- where they get installed, /usr/local/lib/node_modules, use npm root -g to check where
- why do we need global modules
- how they work, node look up them
- where they are used, give example of cli
- how to check what packages are installed globally, npm list -g
---